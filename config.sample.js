// Copy this to config.js for GitHub Pages deployments and set your keys.
// If present, app.js will use these values instead of fetching .env.
window.PUBLIC_CONFIG = {
  SUPABASE_URL: "https://your-project.supabase.co",
  SUPABASE_ANON_KEY: "your-anon-key",
};
